<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:background="@drawable/custom_dialog_notify_bg"
    android:gravity="center"
    android:minHeight="56dp"
    android:minWidth="120dp"
    android:orientation="vertical"
    android:paddingBottom="12dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="12dp">

    <ProgressBar
        android:id="@+id/view_custom_progress_dialog_progress"
        style="?android:attr/progressBarStyle"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/custom_dialog_notify_circular_progress"
        android:visibility="gone" />

   <!--<pl.droidsonroids.gif.GifImageView-->
       <!--android:id="@+id/view_custom_progress_dialog_gif"-->
       <!--android:layout_width="30dp"-->
       <!--android:layout_height="30dp"-->
       <!--android:visibility="gone"/>-->

    <ImageView
        android:id="@+id/view_custom_progress_dialog_img"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/view_custom_progress_dialog_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Loading..."
        android:textColor="@color/white"
        android:textSize="14sp" />

</LinearLayout>